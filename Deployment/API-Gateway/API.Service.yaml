apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: mock-project
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - port: 8080         # cổng service trong cluster
      targetPort: 8080   # cổng container trong Pod